services:
  mysql:
    image: 'mysql:latest'
    restart: no
    environment:
      - 'MYSQL_DATABASE=restdb'
      - 'MYSQL_PASSWORD=password'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=restadmin'
    ports:
      - '3306:3306'